# actions/

Machine-readable contracts for GPT Actions.

- `openapi.yaml`: The API contract imported into GPT Actions.
- Keep descriptions bilingual if possible (CN + EN).
- Changes here should be mirrored in `docs/API_SURFACE.md`.
